// Package controller manages HTTP server lifecycle and provides shared context keys.
package controller

// ContextFieldUserID is a Gin context key for storing the authenticated user's ID.
type ContextFieldUserID struct{}
